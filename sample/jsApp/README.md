# GearUI Sample jsApp

`jsApp` 是 GearUI Sample 的 Web (JS) 宿主工程，基于 Kuikly `h5App` 模板裁剪。

## 运行

```bash
cd sample/jsApp
./gradlew jsBrowserDevelopmentRun
```

## 构建

```bash
cd sample/jsApp
./gradlew jsBrowserProductionWebpack
```

产物默认在 `build/kotlin-webpack/js/productionExecutable`。
